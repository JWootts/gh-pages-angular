export interface GitHubCloneRequest {
    https_url: string;
    ssh_url: string;
    github_cli_url: string;
}

export interface ContactDetails {
    link: string;
    img: string;
    label_tag: string;
}